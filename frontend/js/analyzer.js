// The analyzer page owns the real extraction workflow in analysis-input.js.
// Keep this export for the shared bootstrap; no simulated analysis runs on other pages.
export function initAnalyzer() {}
