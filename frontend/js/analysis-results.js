import {api, escapeHtml as esc, sourceUrl} from './utils/api.js';
import {Storage} from './utils/storage.js';

export const shortText = (text, limit=280) => { const value = String(text || '').replace(/\s+/g,' ').trim(); return value.length > limit ? value.slice(0,limit).replace(/\s+\S*$/, '') + '…' : value; };
export const dateLabel = value => value ? new Date(value).toLocaleString(undefined, {dateStyle:'medium',timeStyle:'short'}) : 'Not recorded';
export const list = values => values.length ? `<ul class="finding-list">${values.map(v=>`<li>${esc(v)}</li>`).join('')}</ul>` : '<p class="muted">Not recorded in this analysis.</p>';
export const badge = status => `<span class="status-label ${status === 'Supported' ? 'supported' : status === 'Not Found' ? 'unavailable' : 'review'}">${esc(status)}</span>`;
export function traceRows(result) {
  return result.traceability?.length ? result.traceability : (result.extracted.key_requirements || []).map(requirement=>({requirement,status:'Review Required',note:'This historical analysis has no verified requirement-to-source mapping.'}));
}
export function traceTable(rows) {
  return `<div class="table-scroll" tabindex="0" role="region" aria-label="Requirement traceability"><table class="trace-table"><thead><tr><th>Requirement</th><th>Standard</th><th>Evidence</th><th>Status</th></tr></thead><tbody>${rows.map(row=>`<tr><td>${esc(row.requirement)}</td><td>${esc(row.is_number || 'Not mapped')}</td><td>${row.source ? `${esc(row.source)} · p. ${Number(row.page)}<small>${esc(shortText(row.excerpt,160))}</small>` : esc(row.note || 'No mapped source passage')}</td><td>${badge(row.status)}</td></tr>`).join('')}</tbody></table></div>`;
}
export function requirementFacts(ext) {
  const facts = [['Product',ext.product_name],['Intended use',ext.application],['Purpose',ext.purpose],['Quantity',ext.quantity],...Object.entries(ext.technical_parameters || {}),['Safety',ext.safety_parameters?.join('; ')]];
  return `<dl class="requirement-facts">${facts.map(([label,value])=>`<div><dt>${esc(label)}</dt><dd>${esc(value || 'Not specified')}</dd></div>`).join('')}</dl>`;
}
export function relatedGroups(items) {
  const groups = [['normative-reference','Normative references'],['testing','Test methods'],['safety','Safety standards'],['installation','Installation standards'],['related','Supporting / referenced standards']];
  return items.length ? groups.filter(([key])=>items.some(i=>i.relationship===key)).map(([key,label])=>`<div class="related-group"><h3>${label}</h3>${items.filter(i=>i.relationship===key).map(i=>`<p><a href="/pages/standard-details.html?id=${encodeURIComponent(i.id)}">${esc(i.is_number)}</a> · ${esc(i.title)}<small>${esc(i.description)}</small></p>`).join('')}</div>`).join('') : '<p class="muted">No explicit relationships were identified in the available source records.</p>';
}
export function versionTable(items) {
  return items.length ? `<div class="table-scroll"><table class="trace-table"><thead><tr><th>Indexed version</th><th>Earlier local editions</th><th>Amendment files</th><th>Verification</th></tr></thead><tbody>${items.map(v=>`<tr><td><b>${esc(v.indexed_version)}</b><small>${esc(v.source)}</small></td><td>${esc(v.previous_versions.join('; ') || 'Not found in available records')}</td><td>${esc(v.amendments.join('; ') || 'Not found in available records')}</td><td>${esc(v.status)}</td></tr>`).join('')}</tbody></table></div>` : '<p class="muted">Not verified in the available knowledge base. Open the standard record to review indexed editions.</p>';
}

function openDrawer(title, html) {
  const prior = document.activeElement;
  const dialog = document.createElement('dialog'); dialog.className = 'analysis-drawer';
  dialog.innerHTML = `<header><div><span class="eyebrow">PRAMAN · ANALYSIS CONTEXT</span><h2>${esc(title)}</h2></div><button class="btn btn-secondary btn-sm" aria-label="Close panel">Close ×</button></header><div class="drawer-content">${html}</div>`;
  document.body.append(dialog);
  dialog.querySelector('header button').onclick = ()=>dialog.close();
  dialog.addEventListener('click', event=>{if(event.target===dialog)dialog.close();});
  dialog.addEventListener('close',()=>{dialog.remove(); prior?.focus();});
  dialog.showModal(); return dialog;
}

export async function renderResults(panel, id) {
  const result = await api('/analysis/' + encodeURIComponent(id));
  const ext=result.extracted, recs=result.recommendations || [], rows=traceRows(result);
  const flags=result.review_flags || [], gaps=result.gaps?.length ? result.gaps : result.completeness?.recommendations_to_improve || [];
  panel.innerHTML = `
    <header class="analysis-heading"><div class="heading-row"><div><span class="eyebrow">PROCUREMENT REVIEW</span><h1>Procurement Standards Analysis</h1></div><span class="completion-label">✓ Analysis complete</span></div><p class="requirement-title">${esc(ext.product_name)}</p><div class="analysis-meta"><span>Analysis ID <b>${esc(result.id)}</b></span><span>Source <b>${esc(result.source_name || ext.source_name || 'Entered requirement')}</b></span><span>Analyzed <b>${esc(dateLabel(result.analyzed_at || ext.extracted_at))}</b></span></div><p class="decision-notice">PRAMAN provides decision support. Verify applicable standards against authoritative BIS records before final procurement approval.</p></header>
    <div class="result-actions"><a class="btn btn-primary" href="/pages/report-view.html?analysis_id=${encodeURIComponent(id)}">Generate Report</a><button class="btn btn-secondary" id="ask-praman">Ask PRAMAN</button><a class="btn btn-secondary" href="/pages/analyze.html">Start New Analysis</a><span class="saved-note">✓ Saved to History</span></div>
    <section class="summary-strip" aria-label="Analysis summary"><div><strong>${ext.key_requirements.length}</strong><span>Requirements identified</span></div><div><strong>${recs.length}</strong><span>Candidate standards</span></div><div><strong>${recs.filter(r=>r.evidence?.length).length}</strong><span>With source evidence</span></div><div><strong>${rows.filter(r=>r.status!=='Supported').length}</strong><span>Requirements to review</span></div></section>
    ${flags.length ? `<aside class="review-strip"><b>Review before procurement approval</b><p>${esc(flags[0])}</p>${flags.length>1 ? `<details><summary>${flags.length-1} additional review flag${flags.length>2?'s':''}</summary>${list(flags.slice(1))}</details>`:''}</aside>`:''}
    <section class="card"><div class="section-heading"><h2>Extracted Procurement Requirements</h2><a href="/pages/review.html?extraction_id=${encodeURIComponent(ext.id)}">Review &amp; reanalyze →</a></div>${requirementFacts(ext)}<form id="category-form" class="category-row"><label for="detected-category">Detected category</label><input id="detected-category" class="form-input" maxlength="160" value="${esc(result.category || ext.category)}" placeholder="Not detected — enter a domain"><button class="btn btn-secondary btn-sm" type="submit">Save category</button><span id="category-status" role="status"></span></form><details class="secondary-details"><summary>View all ${ext.key_requirements.length} extracted requirements and input</summary>${list(ext.key_requirements)}<details><summary>Original input</summary><pre class="source-text input-preview">${esc(ext.source_text)}</pre></details></details>${ext.warning ? `<p class="helper">${esc(ext.warning)}</p>`:''}</section>
    <section class="card"><div class="section-heading"><h2>Requirement → Standard Traceability</h2><span class="muted">Source coverage, subject to review</span></div>${rows.length ? traceTable(rows.slice(0,6)) : '<p>No explicit requirements were extracted.</p>'}${rows.length>6 ? `<details class="secondary-details"><summary>View ${rows.length-6} more requirements</summary>${traceTable(rows.slice(6))}</details>`:''}</section>
    <section class="recommendations-section"><div class="section-heading"><h2>Recommended Indian Standards</h2><span class="muted">${recs.length} candidate${recs.length===1?'':'s'}</span></div><p class="helper">Recommendations indicate source relevance. Applicability and current validity require officer review.</p>
    ${recs.length ? recs.map((rec,index)=>{
      const addressed=[...new Set(rows.filter(r=>r.standard_id===rec.id).map(r=>r.requirement))];
      return `<article class="standard-card"><div class="heading-row"><div><h3>${esc(rec.is_number)}</h3><p class="standard-title">${esc(rec.title)}</p></div><span class="relevance-label">${esc(rec.relevance.replaceAll('-',' '))} relevance</span></div><p class="match-reason"><b>Why it matches</b> ${esc(shortText(rec.reasons?.[0] || 'Source passages retrieved; review their scope.'))}</p><div class="requirements-addressed"><span>Requirements addressed</span>${addressed.length ? addressed.slice(0,3).map(r=>`<span class="requirement-tag">${esc(shortText(r,75))}</span>`).join('') : '<span class="muted">Coverage needs review</span>'}${addressed.length>3?`<span>+${addressed.length-3} in traceability</span>`:''}</div><footer><span class="muted">${new Set(rec.evidence.map(e=>e.source)).size} source files · ${rec.evidence.length} passages · Current status unverified</span><div><button class="btn btn-secondary btn-sm" data-evidence="${index}">View evidence</button><a class="btn btn-secondary btn-sm" href="/pages/standard-details.html?id=${encodeURIComponent(rec.id)}">View standard</a><button class="btn btn-secondary btn-sm" data-save="${esc(rec.id)}">${Storage.isStandardSaved(rec.id)?'Saved':'Save'}</button></div></footer></article>`;
    }).join('') : '<div class="card empty-result"><h3>No sufficiently relevant standard was identified</h3><p>The available knowledge base did not return a suitable candidate. Review the product description or add relevant source documents.</p></div>'}</section>
    <section class="card"><details><summary class="section-summary">Related Standards <span>${result.related_standards.length} records</span></summary>${relatedGroups(result.related_standards)}${result.graph.edges.length ? `<details class="secondary-details"><summary>View relationship map</summary><div class="relationship-map">${result.graph.edges.map(edge=>`<div><b>${esc(result.graph.nodes.find(n=>n.id===edge.source)?.is_number || edge.source)}</b><span aria-label="references">→</span><b>${esc(result.graph.nodes.find(n=>n.id===edge.target)?.is_number || edge.target)}</b><small>${esc(edge.relationship)}</small></div>`).join('')}</div></details>`:''}</details></section>
    <section class="card"><details><summary class="section-summary">Version &amp; Amendment Review <span>Verification required</span></summary>${versionTable(result.version_findings || [])}</details></section>
    <section class="card"><details><summary class="section-summary">Specification Gaps <span>${gaps.length} items for review</span></summary><p class="helper">Missing input fields are clarification prompts; they do not establish a failure to comply.</p>${gaps.length?list(gaps):'<p>No missing fields were identified. Adequacy still requires review.</p>'}</details></section>
    ${result.applicability?.length ? `<section class="card"><details><summary class="section-summary">Applicability Review <span>Referenced standards</span></summary>${result.applicability.map(a=>`<h3>${esc(a.referenced_standard)}</h3><dl class="requirement-facts"><div><dt>Product scope</dt><dd>${esc(a.product_scope)}</dd></div><div><dt>Application</dt><dd>${esc(a.application)}</dd></div><div><dt>Technical characteristics</dt><dd>${esc(a.technical_characteristics)}</dd></div></dl><p>${esc(a.overall)}</p>`).join('')}</details></section>`:''}
    <details class="audit-details"><summary>Analysis record &amp; export</summary><p>Retrieval: ${esc(result.retrieval_mode)} · Reasoning: ${esc(result.generation_mode)}</p><p>${esc(shortText(result.explanation,600))}</p><button class="btn btn-secondary btn-sm" id="export-analysis">Export analysis JSON</button></details>
  `;
  panel.querySelectorAll('[data-save]').forEach(button=>button.onclick=()=>{button.textContent=Storage.toggleSaveStandard(button.dataset.save)?'Saved':'Save';});
  panel.querySelectorAll('[data-evidence]').forEach(button=>button.onclick=()=>{
    const rec=recs[Number(button.dataset.evidence)];
    openDrawer('Evidence · '+rec.is_number, `<p class="helper">Passages retrieved from indexed documents. Page numbers refer to physical document pages.</p>${rec.evidence.map((ev,i)=>`<article class="evidence-item"><h3>Source ${i+1} · ${esc(ev.source)}</h3><dl class="evidence-meta"><div><dt>Page</dt><dd>${Number(ev.page)}</dd></div><div><dt>Section / chunk</dt><dd>${esc(ev.section || ev.citation_id)}</dd></div></dl><blockquote>${esc(shortText(ev.text,360))}</blockquote><a href="${esc(sourceUrl(ev))}" target="_blank" rel="noopener">Open source ↗</a><details><summary>View full evidence</summary><p class="source-text">${esc(ev.text)}</p><small>${esc(ev.citation_id)}</small></details></article>`).join('') || '<p>No source evidence is available.</p>'}`);
  });
  panel.querySelector('#category-form').onsubmit=async event=>{
    event.preventDefault(); const button=event.currentTarget.querySelector('button'), status=panel.querySelector('#category-status'); button.disabled=true;
    try {await api('/analysis/'+encodeURIComponent(id)+'/category',{method:'PATCH',body:{category:panel.querySelector('#detected-category').value.trim()}});status.textContent='Category saved.';}catch(error){status.textContent=error.message;}finally{button.disabled=false;}
  };
  panel.querySelector('#ask-praman').onclick=()=>{
    const dialog=openDrawer('Ask PRAMAN', `<p>Ask about the requirement, recommendations and evidence in this analysis.</p><form id="context-question"><label for="analysis-question">Your question</label><textarea id="analysis-question" class="form-textarea" rows="3" maxlength="2000" required placeholder="Which evidence supports this recommendation?"></textarea><button class="btn btn-primary" type="submit">Ask PRAMAN</button></form><div class="context-answer" role="status"></div>`);
    dialog.querySelector('form').onsubmit=async event=>{event.preventDefault();const button=dialog.querySelector('form button'),answer=dialog.querySelector('.context-answer');button.disabled=true;answer.textContent='Checking the sources in this analysis…';try{const data=await api('/analysis/'+encodeURIComponent(id)+'/ask',{method:'POST',body:{question:dialog.querySelector('textarea').value}});answer.textContent=data.answer;}catch(error){answer.textContent=error.message;}finally{button.disabled=false;}};
  };
  panel.querySelector('#export-analysis').onclick=async()=>{const current=await api('/analysis/'+encodeURIComponent(id));const url=URL.createObjectURL(new Blob([JSON.stringify(current,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='PRAMAN_'+id+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);};
}
